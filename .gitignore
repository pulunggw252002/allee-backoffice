node_modules/
.next/
out/
.env*.local
.DS_Store
*.log
next-env.d.ts

# Local agent/launcher state — not portable
.claude/scheduled_tasks.lock

# SQLite database — never commit. Generated by `npm run db:migrate` + seeded
# locally via `npm run db:seed`. Each developer/host gets their own.
data/*.db
data/*.db-shm
data/*.db-wal

# TypeScript incremental build info
*.tsbuildinfo

.vercel
